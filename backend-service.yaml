apiVersion: v1
kind: Service
metadata:
  name: bike-backend-service
spec:
  type: NodePort
  selector:
    app: bike-backend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30007

